#!/bin/bash

# Installs commonly needed build tools, through Brew

brew install protobuf
brew install Caskroom/cask/java
brew install maven
brew install gradle
brew install node
brew install sbt
npm install -g bower
npm install -g grunt-cli
